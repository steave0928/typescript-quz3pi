var title: string = 'Angular!';
title = 'Angular2';

var n: number = 0;

var isOpen: Boolean = true;

// 配列
var keywords: (string | number)[] = ['name', 'email', 'body', 0];

//タプル
var pyment: [number, string, number] = [1, 'apple', 2];
